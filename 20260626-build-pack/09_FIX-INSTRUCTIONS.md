# 09. 修正指示書(再レビュー後の残課題)

対象: `aidea-workshop-20260626/`(2026-06-12再レビューで条件付き合格)
前回の再作業(08)はほぼ完了している。本書は残り4件のピンポイント修正のみを指示する。
**08で合格した部分(facilitator-guide本文、instructor-notes、実物素材、デモプロジェクト)は変更しない。**

## FIX-1(必須): スライドに混入した制作指示文の除去

Slide 13 / 14 / 16 / 19 / 23 の右カラム注釈に、制作時の内部指示
「実際に採取したログの特徴行を表示。黒箱は内容量に合わせて高さを変え、空白で水増ししない。」
がそのまま載っており、聴衆に見える。各スライドから**この文を削除**する(注釈の先頭にある本来のキャプションは残す)。

あわせて Slide 19 の黒箱(ターミナル風ボックス)内の最終行
`captured from red-to-green.txt`
を削除する。出所メモはスライドに不要(話者ノート側に書くのは可)。

削除後、本来のキャプションが欠けるスライドがあれば以下を使う:

- 13: 小計6,000円で送料500円なのにメッセージは「送料無料」。片方の閾値だけが変わった。
- 14: 境界値4999/5000と不変条件を、現在の挙動のまま固定した。
- 16: テストは全部緑。でも構造のセンサーだけが赤を出している。
- 19: Demo 1で人間がcurlで見つけた矛盾を、今回はテストが自動で捕まえた。
- 23: 人間が読むのはこの報告とdiffだけ。介入は入口1回+出口1回。

## FIX-2(必須): 話者ノートの定型文使い回し解消

全28枚のノート後半「進行注意: 読み上げで埋めず、重要語の前後で一呼吸置き、〜」が同一文。
各スライド固有の進行注意(問いかけのタイミング、ターミナルへの切り替え、間の取り方、次スライドへの繋ぎ)に書き換える。
基準: **10文字超の同一文が3枚以上のノートに出現しないこと。**

## FIX-3(必須): typecheckのタイムアウト是正と降格の可視化

- `TSC_TIMEOUT_MS = 900` ではtscの起動(通常1〜2秒)が常に時間切れになり、typescriptが入った本番マシンでもWARN降格する。**10000msへ変更**(03/04/05の全project)。
- A4の「sensors 5秒以内」はtsc実行時に超え得るため、検収基準を「typecheckを除き5秒以内。typecheckは15秒以内」と読み替え、verify-kitのA4を修正する。
- run-sensors.js: 子センサーの出力に `WARN` が含まれる場合、サマリ表の下に該当WARN行を再掲する(表の緑だけ見て降格に気づけない問題への対処)。
- 修正後、tscの有無それぞれの環境での動作確認結果をBUILD-NOTESに記録する(tscが用意できない場合はその旨を未完了項目に書く。「なし」と書かない)。

## FIX-4(必須): リハーサル記録の整合とC1(Demo 2)の実施

- BUILD-NOTESのC3「反復回数: 1」が実物(loop-state 3行 / completion-report「反復回数: 3」)と不一致。実物に合わせて修正する。
- 07の定義では C1=Demo 2 / C2=Demo 4 / C3=Demo 5。現記録の「C1 Demo 1」は **M1/M2採取記録** に改名して残し、本来のC1として **Demo 2のリハーサル(新規エージェントセッションにrun-demo.mdのみを与え、characterizationテストを生成させる)** を実施する。
  - 合格条件: 生成テストが全緑、境界値4999/5000と不変条件を含む。
  - セッションログを `02_characterization/instructor-materials/session-log.txt` に保存し、BUILD-NOTESにC1記録(日時/プロンプト/結果/調整点)を追加する。
  - 実エージェントセッションが実行できない環境の場合は、未完了項目に「C1は手動相当で代替」と明記する(黙って「相当」で済ませない)。

## verify-kitへの追加(FIX-1/2の再発防止)

- **混入語彙チェック**: 全スライドXMLのテキストに `黒箱|水増し|captured from|プレースホルダ|レンダリング|採取した` が含まれたらFAIL(話者ノートは対象外)。
- **ノート重複チェックの強化**: 正規化後10文字超の同一文が3枚以上に出現したらFAIL(現行の検査はこれを素通りした)。

## 再修正後の確認(再レビューで実行されるコマンド)

1. `bash scripts/verify-kit.sh` 全PASS(追加チェック込み)
2. PPTX展開後 `grep -lE '黒箱|水増し|captured from' ppt/slides/*.xml` → ヒット0
3. ノート28枚から文を抽出し重複カウント → 3回以上の同一文が0
4. `grep TSC_TIMEOUT demo-units/0{3,4,5}*/project/scripts/typecheck.js` → 全て10000
5. BUILD-NOTES: C1(Demo 2)記録が存在し、C3の反復回数が実物と一致
6. qa/ の再レンダリング画像で Slide 13/14/16/19/23 を目視確認(混入文なし)
