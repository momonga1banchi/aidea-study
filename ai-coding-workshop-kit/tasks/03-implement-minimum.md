# Task 03: Implement Minimum Code

Read:

- `AGENTS.md`
- `docs/specs/member-discount.md`

Task:
Make the failing tests pass with the minimum production code.

Constraints:

- Follow the existing route/controller/service structure.
- Keep business logic in services.
- Keep controllers focused on HTTP request/response handling.
- Do not add dependencies.
- Do not change existing public API response formats.
- Do not modify unrelated files.

After implementation:

1. Run `npm test`
2. Run `npm run lint`
3. Fix failures if any
4. Summarize the diff
5. List risks and follow-up items
