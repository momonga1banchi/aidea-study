# Demo 4 red to green rehearsal
executed_at: 2026-06-16T12:00:00+09:00
prompt: AGENTS.mdを読んだ上で、CR-2026-06-26-free-shipping-threshold.md の変更を実装してください。
iteration_count: 2

$ perl -0pi -e "s/FREE_SHIPPING_THRESHOLD = 5000/FREE_SHIPPING_THRESHOLD = 7000/" src/config/pricing.js

$ node --test
TAP version 13
# Subtest: POST /orders/estimate returns free shipping at threshold
not ok 4 - POST /orders/estimate returns free shipping at threshold
  ---
  error: |-
    Expected values to be strictly equal:

    500 !== 0
  ...
# Subtest: estimateOrder applies free shipping at 5000
not ok 12 - estimateOrder applies free shipping at 5000
  ---
  error: |-
    Expected values to be strictly equal:

    500 !== 0
  ...
1..21
# tests 21
# pass 19
# fail 2
[exit 1]

reading:
- これは実装失敗ではなく、旧5,000円境界を固定していたcharacterization testが赤になった状態。
- response形式、不正入力、total計算は残す。
- 4999/5000の境界テストは、6999/7000の新仕様受け入れテストへ書き換える。
- docsが古かったことと、5,000円から7,000円へ変えた判断はworklog/ADRへ記録する。

$ rg "5000|4999|7000|6999" src tests docs
src/config/pricing.js:const FREE_SHIPPING_THRESHOLD = 7000;
tests/orderEstimate.api.test.js: paid shipping below new threshold uses 6999
tests/orderEstimate.api.test.js: free shipping at new threshold uses 7000
tests/orderEstimateService.test.js: estimateOrderBySubtotal mirrors new threshold at 7000
docs/specs/order-estimate.md:送料無料条件は小計7,000円以上

$ npm run sensors

| sensor | status | durationMs |
|---|---:|---:|
| test | green | 441 |
| lint | green | 344 |
| typecheck | green | 1499 |
| policy-boundary | green | 351 |
| api-response | green | 345 |

All sensors are green.
[exit 0]

result:
CRの受け入れ条件、policy境界、APIレスポンス形式、spec/ADR/worklogが揃ったため、ワークショップ上はリリース候補としてレビューできる状態。
